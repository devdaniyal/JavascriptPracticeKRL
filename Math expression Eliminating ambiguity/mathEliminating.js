function helloWorld(){
    var num = 1 + 3 * 4;
    // we expecting answer 16 but its 13 becz javasript consider bodmas rule.
    // console.log('num',num); 
    var profit = 4 * (1 + 3); 
    // it will answer 16
    // console.log('profit',profit);
    var result = (2 * 4) * (4 + 2);
    // console.log("result", result);
}
helloWorld()